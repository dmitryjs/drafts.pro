# Быстрый старт

## Что было сделано

✅ Проект настроен для работы с **Supabase** вместо локального PostgreSQL  
✅ Все необходимые переменные окружения настроены  
✅ Клиент уже использует Supabase для аутентификации  
✅ Сервер использует Supabase PostgreSQL через Drizzle ORM  

## Что нужно сделать СЕЙЧАС

### 1. Создайте проект в Supabase

1. Перейдите на https://supabase.com и создайте аккаунт (бесплатно)
2. Создайте новый проект
3. **Сохраните пароль базы данных** - он понадобится!

### 2. Получите данные для .env файла

**В Supabase Dashboard:**

1. **Settings → Database → Connection string → URI**
   - Скопируйте строку подключения
   - Замените `[YOUR-PASSWORD]` на ваш реальный пароль

2. **Settings → API**
   - Скопируйте **Project URL**
   - Скопируйте **anon public** ключ

### 3. Обновите файл .env

Откройте файл `.env` в корне проекта и замените значения:

```env
# Замените на вашу строку подключения из Supabase
DATABASE_URL=postgresql://postgres:ВАШ_ПАРОЛЬ@db.xxx.supabase.co:5432/postgres

# Замените на ваши значения из Settings → API
VITE_SUPABASE_URL=https://xxx.supabase.co
VITE_SUPABASE_ANON_KEY=ваш_anon_ключ

# Если у вас есть ключ Polza AI
POLZA_AI_API_KEY=ваш_ключ

NODE_ENV=development
PORT=5000
```

### 4. Создайте таблицы в базе данных

Выполните миграции:

```bash
npm run db:push
```

Эта команда создаст все необходимые таблицы в Supabase.

### 5. Запустите проект

```bash
npm run dev
```

Проект будет доступен на **http://localhost:5000**

---

## Важные моменты

🔒 **Безопасность:**
- `DATABASE_URL` и `POLZA_AI_API_KEY` - **секретные**, только на сервере
- `VITE_SUPABASE_ANON_KEY` - публичный, безопасно для клиента

💰 **Стоимость:**
- Supabase Free tier: 500MB базы данных, 2GB bandwidth - **достаточно для MVP**
- При росте можно перейти на Pro ($25/месяц)

📦 **Для продакшена:**
- Frontend: Vercel (бесплатно для статики)
- API: Vercel Serverless Functions (бесплатно до 100GB-hours)
- База данных: Supabase (Free tier или Pro)

---

## Если что-то не работает

1. **Проверьте .env файл** - все переменные должны быть заполнены
2. **Проверьте подключение к Supabase** - база данных должна быть активна
3. **Проверьте миграции** - выполните `npm run db:push` еще раз
4. **Проверьте логи** - в консоли будут видны ошибки

Подробная инструкция: см. `SETUP_SUPABASE.md`
