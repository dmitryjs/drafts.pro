## Packages
framer-motion | Smooth page transitions and micro-interactions
recharts | Visualization for user progress and stats
lucide-react | Beautiful icons for the UI (already in base but ensuring version match)

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  display: ["var(--font-display)"],
  body: ["var(--font-body)"],
  mono: ["var(--font-mono)"],
}
