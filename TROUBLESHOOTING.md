# Решение проблем с подключением к базе данных

## ✅ Что уже работает

- ✅ Таблицы созданы в Supabase (24 таблицы)
- ✅ Сервер запускается на http://localhost:5000
- ✅ Авторизация через Supabase работает

## ⚠️ Текущая проблема

Ошибка DNS при подключении к базе данных:
```
Error: getaddrinfo ENOTFOUND db.ionriyybabiuebxwmvoc.supabase.co
```

## 🔧 Решения

### Вариант 1: Проверьте подключение к Supabase

1. Откройте Supabase Dashboard
2. Перейдите в **Settings → Database**
3. Проверьте, что база данных **активна** (не приостановлена)
4. Если база данных приостановлена - нажмите "Resume"

### Вариант 2: Проверьте строку подключения

1. В Supabase Dashboard → **Settings → Database**
2. Найдите **Connection string** → **URI**
3. Убедитесь, что строка подключения правильная:
   ```
   postgresql://postgres:[PASSWORD]@db.ionriyybabiuebxwmvoc.supabase.co:5432/postgres
   ```
4. Проверьте, что пароль правильный

### Вариант 3: Используйте Connection Pooling (рекомендуется)

В Supabase есть **Connection Pooling** - это более надежный способ подключения:

1. В Supabase Dashboard → **Settings → Database**
2. Найдите **Connection Pooling**
3. Скопируйте строку подключения с **pooler** (порт 6543 вместо 5432)
4. Обновите в `.env`:
   ```
   DATABASE_URL=postgresql://postgres.[PROJECT-REF]:[PASSWORD]@aws-0-[REGION].pooler.supabase.com:6543/postgres
   ```

### Вариант 4: Проверьте интернет-соединение

- Убедитесь, что интернет работает
- Попробуйте открыть https://supabase.com в браузере
- Проверьте, нет ли блокировки файрвола

## 📝 Важно

**Сервер работает**, но не может подключиться к базе данных. Это означает:
- ✅ Интерфейс открывается
- ✅ Авторизация работает (через Supabase Auth)
- ❌ Сохранение данных не работает (требует подключения к БД)

## 🚀 После исправления

1. Перезапустите сервер:
   ```bash
   npm run dev
   ```

2. Попробуйте сохранить профиль - должно работать!

3. Проверьте данные в Supabase Dashboard → Table Editor

---

**Рекомендация:** Используйте Connection Pooling (вариант 3) - это более надежно для продакшена.
