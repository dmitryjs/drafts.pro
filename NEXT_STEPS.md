# Что делать дальше - Пошаговая инструкция

## ✅ Текущий статус

- ✅ Код переведен на Supabase API
- ✅ Сервер запущен на http://localhost:5000
- ✅ Service Role Key добавлен в `.env`
- ✅ Ошибка ENOTFOUND больше не должна появляться

## 🧪 Шаг 1: Проверьте, что сервер работает

1. Откройте браузер
2. Перейдите на http://localhost:5000
3. Должна открыться главная страница приложения

**Если страница не открывается:**
- Проверьте, что сервер запущен (в терминале должно быть `serving on localhost:5000`)
- Если нет - запустите: `npm run dev`

## 🔐 Шаг 2: Проверьте авторизацию

1. Перейдите на страницу авторизации (обычно `/auth`)
2. Введите email
3. Нажмите "Отправить код" (OTP) или "Войти через Google"
4. Проверьте email и введите код (если OTP)
5. Должна произойти авторизация

**Если авторизация не работает:**
- Проверьте консоль браузера (F12 → Console) на ошибки
- Убедитесь, что в `.env` есть `VITE_SUPABASE_URL` и `VITE_SUPABASE_ANON_KEY`

## 💾 Шаг 3: Проверьте сохранение профиля

1. После авторизации перейдите в профиль
2. Измените какое-то поле (например, имя или биографию)
3. Нажмите "Сохранить"
4. Должно появиться сообщение "Профиль сохранён!"

**Если сохранение не работает:**
- Откройте консоль браузера (F12 → Console)
- Посмотрите на ошибки - там будет точная причина
- Убедитесь, что в `.env` есть `SUPABASE_SERVICE_ROLE_KEY`

## ✅ Шаг 4: Проверьте, что ошибка ENOTFOUND исчезла

1. Откройте терминал, где запущен сервер
2. Посмотрите на логи - не должно быть ошибок `ENOTFOUND db.*.supabase.co`
3. Если ошибки есть - перезапустите сервер: `Ctrl+C` и `npm run dev`

## 🎯 Если всё работает

**Поздравляю!** MVP готов к тестированию:
- ✅ Авторизация работает
- ✅ Профили сохраняются
- ✅ Нет ошибок подключения к БД

## ⚠️ Если что-то не работает

### Проблема: "Supabase not configured"
**Решение:** Проверьте `.env` - должны быть:
- `VITE_SUPABASE_URL`
- `VITE_SUPABASE_ANON_KEY`
- `SUPABASE_SERVICE_ROLE_KEY`

### Проблема: "Profile not found" или ошибка 404
**Решение:** Профиль создается автоматически при первом входе. Попробуйте:
1. Выйти и войти снова
2. Проверить в Supabase Dashboard → Table Editor, есть ли таблица `profiles`

### Проблема: "Permission denied" или ошибка RLS
**Решение:** Убедитесь, что в `.env` есть `SUPABASE_SERVICE_ROLE_KEY` (не только `VITE_SUPABASE_ANON_KEY`)

### Проблема: Сервер не запускается
**Решение:**
1. Остановите все процессы Node.js: `Get-Process | Where-Object {$_.ProcessName -eq "node"} | Stop-Process -Force`
2. Запустите снова: `npm run dev`

## 📝 Что дальше?

После того, как всё работает:
1. Протестируйте основные функции
2. Проверьте, что данные сохраняются в Supabase Dashboard → Table Editor
3. Если нужно - добавьте реализацию задач/батлов через Supabase API (пока они возвращают пустой массив)

---

**Готово!** Теперь можно тестировать MVP и проверять гипотезу.
