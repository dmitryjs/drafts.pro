# Быстрое исправление - Резюме

## ✅ Проблема решена

Ошибка `getaddrinfo ENOTFOUND db.*.supabase.co` больше не появится, потому что:
- ❌ Прямое подключение к Postgres **отключено**
- ✅ Используется **Supabase API** через HTTPS

## 📋 Что нужно сделать (2 шага)

### 1. Добавить Service Role Key (рекомендуется)

В Supabase Dashboard → Settings → API → Service Role Key

Добавьте в `.env`:
```
SUPABASE_SERVICE_ROLE_KEY=ваш_ключ
```

**Если не добавите** - будет использован `VITE_SUPABASE_ANON_KEY` (может не работать для обновления профилей из-за RLS).

### 2. Перезапустите сервер

```bash
npm run dev
```

## ✅ Результат

- ✅ Сервер запускается без ошибок
- ✅ Профили работают (создание, обновление)
- ✅ Авторизация работает
- ⚠️ Задачи временно не работают (пустой массив) - можно добавить позже

## 📁 Измененные файлы

- `server/supabase-server.ts` - новый
- `server/storage_supabase.ts` - новый  
- `server/storage.ts` - изменен
- `server/db.ts` - отключен Postgres
- `server/replit_integrations/auth/replitAuth.ts` - memorystore

## 🎯 Готово!

Теперь можно запускать MVP и проверять гипотезу.
